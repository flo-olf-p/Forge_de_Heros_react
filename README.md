# Project "Forge de Héros"

## Installation :
- After cloning the repo, use 'npm i' command to install all dependencies.
- Then use 'npm run build' to build vite project
- Run 'npm run preview' to start the app.
